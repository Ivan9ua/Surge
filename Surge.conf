# Surge Mac 配置模板
# 公开脱敏模板：请替换占位符，并在 Surge UI 中配置设备证书与 Keystore。

[General]
#!include Shared-General.dconf
# macOS 专用：ICMP 转发与 /etc/hosts 读取
icmp-forwarding = true
read-etc-hosts = true
# macOS 专用：IPv6 保持开启
ipv6 = true
ipv6-vif = auto

[Ponte]
server-proxy-name= HK

[Testing]
# 测试流量限制
download-duration-limit= 5
upload-size-limit= 104857600
upload-duration-limit= 5

[Proxy]
#!include Shared-Routing.dconf

[Proxy Group]
#!include Shared-Routing.dconf

[Rule]
#!include Shared-Routing.dconf

[MITM]
h2 = true
